const COURSE={id:'career-fluency',title:'English Communication & Career Fluency',level:'A2+ → B1',week:1,lessons:[
{id:'w1l1',number:1,title:'Your starting point',outcome:'Complete a friendly speaking diagnostic and identify one clear fluency goal.',expressions:[
{text:'Let me think for a moment.',job:'need time without becoming silent'},
{text:'What I mean is…',job:'restate an idea more clearly'},
{text:'Could you repeat the question?',job:'need to hear a question again'},
{text:'In my experience…',job:'connect an answer to real life'},
{text:'One goal I have is…',job:'state a learning or career goal'}],
listening:{title:'English goals',text:'Tutor: Tell me a little about yourself and how you use English. Student: In my experience, I use English at work, online and when I travel. Tutor: What would you most like to improve? Student: Let me think for a moment. One goal I have is to speak with fewer pauses. Tutor: When do you feel most confident? Student: I feel confident when I read or listen, but spontaneous speaking is harder.',questions:[
{q:'What is the student’s main goal?',options:['Read faster','Speak with fewer pauses','Write longer reports'],answer:'Speak with fewer pauses',tag:'listening:main-idea'},
{q:'Where does the student use English?',options:['At work, online and when travelling','Only at school','Only when travelling'],answer:'At work, online and when travelling',tag:'listening:detail'},
{q:'When does the student feel most confident?',options:['When reading or listening','When presenting','When writing reports'],answer:'When reading or listening',tag:'listening:detail'}]},
grammar:{focus:'Complete simple sentences; accuracy is observed but not over-taught.',items:[
{q:'Choose the complete sentence.',options:['I use English at work.','I English at work.','Use English work.'],answer:'I use English at work.',tag:'grammar:complete-sentence'},
{q:'Choose the accurate goal sentence.',options:['One goal I have is speak more confident.','One goal I have is to speak more confidently.','One goal have speaking confidently.'],answer:'One goal I have is to speak more confidently.',tag:'grammar:infinitive'},
{q:'Choose the accurate question.',options:['Where you use English?','Where do you use English?','Where does you use English?'],answer:'Where do you use English?',tag:'grammar:question-form'}]},
writing:{min:40,max:80,prompt:'Write about where you use English, one thing you already do well, one difficult speaking situation, and one clear 12-week goal.',starters:'In my experience… · I feel confident when… · A difficult situation for me is… · One goal I have is…'},
review:{keywords:'use · strength · goal',mission:'Prepare a two-minute baseline. Speak from three keywords rather than a full script.'}},
{id:'w1l2',number:2,title:'A strong professional introduction',outcome:'Introduce yourself with a clear beginning, useful details and a forward-looking ending.',expressions:[
{text:'I currently work as…',job:'state your present role'},
{text:'My background is in…',job:'describe education or professional field'},
{text:'I’m responsible for…',job:'explain a main duty'},
{text:'I’m particularly interested in…',job:'name a professional interest'},
{text:'My current goal is to…',job:'finish with a future direction'}],
listening:{title:'Professional introduction',text:'Amina: It’s good to meet you. What do you do? Hassan: I currently work as a project assistant. My background is in construction planning. Amina: What are you responsible for? Hassan: I’m responsible for checking schedules and preparing weekly updates. Amina: What would you like to do next? Hassan: My current goal is to lead larger projects and communicate confidently with international teams.',questions:[
{q:'What is Hassan’s current role?',options:['Project assistant','Construction manager','English teacher'],answer:'Project assistant',tag:'listening:detail'},
{q:'What is his background?',options:['Construction planning','Finance','Marketing'],answer:'Construction planning',tag:'listening:detail'},
{q:'What is one future goal?',options:['Lead larger projects','Change careers immediately','Stop preparing updates'],answer:'Lead larger projects',tag:'listening:main-idea'}]},
grammar:{focus:'Present simple with be, have and work; because for reasons.',items:[
{q:'Choose the accurate sentence.',options:['I currently work as a project assistant.','I currently works as a project assistant.','I am currently work as a project assistant.'],answer:'I currently work as a project assistant.',tag:'grammar:present-simple'},
{q:'Choose the correct responsibility sentence.',options:['I’m responsible for checking schedules.','I responsible checking schedules.','I’m responsible to checking schedules.'],answer:'I’m responsible for checking schedules.',tag:'grammar:collocation'},
{q:'Choose the best reason.',options:['I enjoy my role because I solve problems.','I enjoy my role because solve problems.','I enjoy my role because of I solve problems.'],answer:'I enjoy my role because I solve problems.',tag:'grammar:because'}]},
writing:{min:50,max:90,prompt:'Write a professional introduction with your role, background, one responsibility, one professional interest and one future goal.',starters:'I currently work as… · My background is in… · I’m responsible for… · I’m particularly interested in… · My current goal is to…'},
review:{keywords:'role · background · responsibility · goal',mission:'Prepare a 90-second professional introduction without reading full sentences.'}},
{id:'w1l3',number:3,title:'Keep speaking without long pauses',outcome:'Use thinking phrases, connectors and repair strategies to maintain your turn.',expressions:[
{text:'There are two main reasons.',job:'organise an answer before giving details'},
{text:'The first point is…',job:'start your first idea'},
{text:'Another thing is…',job:'add a second idea'},
{text:'For example…',job:'support an idea with evidence'},
{text:'Let me say that another way.',job:'repair an unclear sentence'}],
listening:{title:'Why English matters',text:'Tutor: Why is English important for your career? Student: There are two main reasons. The first point is communication at work. Tutor: Can you give me an example? Student: For example, I sometimes need to speak with people from different countries. Tutor: And the second reason? Student: Another thing is career growth. Let me say that another way: English can open more professional opportunities.',questions:[
{q:'What is the first reason English is important?',options:['Communication at work','Travel discounts','Writing faster'],answer:'Communication at work',tag:'listening:sequence'},
{q:'What example does the student give?',options:['Speaking with people from different countries','Reading novels','Taking exams'],answer:'Speaking with people from different countries',tag:'listening:detail'},
{q:'What is the second reason?',options:['Career growth','Better handwriting','More free time'],answer:'Career growth',tag:'listening:sequence'}]},
grammar:{focus:'Join ideas with and, but, because and so.',items:[
{q:'Choose the best connector: I use English at work ___ I speak with international clients.',options:['because','but','so'],answer:'because',tag:'grammar:connector-because'},
{q:'Choose the best connector: I understand the topic, ___ I sometimes need more time to answer.',options:['but','because','and'],answer:'but',tag:'grammar:connector-but'},
{q:'Choose the best connector: I want to work internationally, ___ I practise English every day.',options:['so','but','because'],answer:'so',tag:'grammar:connector-so'}]},
writing:{min:60,max:100,prompt:'Write “Why I am learning English now.” Give two reasons and at least one example. Connect your ideas with and, but, because or so.',starters:'There are two main reasons. · The first point is… · For example… · Another thing is…'},
review:{keywords:'reason 1 · example · reason 2',mission:'Prepare a two-minute talk: “Why I am learning English now.” Repeat it with fewer pauses.'}}
]};

const NAV={student:[['home','⌂','Home'],['workbook','▤','Workbook'],['progress','◔','Progress'],['leaderboard','★','Leaderboard'],['profile','○','Profile']],teacher:[['teacher-home','⌂','Dashboard'],['students','◎','Students'],['classes','▤','Classes'],['weaknesses','△','Weaknesses'],['reports','▦','Reports'],['teacher-leaderboard','★','Leaderboard']]};
let session=null,currentPage='home',activeLessonId='w1l1',currentStep='vocabulary',apiDB=null;
function getDB(){return apiDB||{version:3,users:[],classes:[],profiles:{},attempts:[],completion:{},writing:{}}}
function saveDB(db){apiDB=db}
function $(id){return document.getElementById(id)} function lesson(id=activeLessonId){return COURSE.lessons.find(l=>l.id===id)||COURSE.lessons[0]} function cap(s){return s.charAt(0).toUpperCase()+s.slice(1)}
async function api(path,options={}){const opts={credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})},...options};const res=await fetch(path,opts);let data={};try{data=await res.json()}catch{}if(!res.ok)throw new Error(data.error||'Request failed');return data}
async function refreshState(){apiDB=await api('/api/state');return apiDB}
function init(){$('loginForm').addEventListener('submit',e=>{e.preventDefault();login($('username').value.trim(),$('password').value)});$('logoutBtn').addEventListener('click',logout);$('menuBtn').addEventListener('click',()=>document.querySelector('.sidebar').classList.toggle('open'))} document.addEventListener('DOMContentLoaded',init);
async function login(username,password){$('loginError').textContent='';try{const r=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username,password})});session=r.user;await refreshState();$('loginScreen').classList.add('hidden');$('app').classList.remove('hidden');$('sidebarName').textContent=session.name;$('sidebarRole').textContent=session.role==='teacher'?'Teacher':'Student';$('sidebarAvatar').textContent=session.name[0];currentPage=session.role==='teacher'?'teacher-home':'home';renderNav();renderPage()}catch(e){$('loginError').textContent=e.message||'Username or password is incorrect.'}}
async function logout(){try{await api('/api/auth/logout',{method:'POST'})}catch{}session=null;apiDB=null;$('app').classList.add('hidden');$('loginScreen').classList.remove('hidden');$('password').value=''}
function renderNav(){const html=NAV[session.role].map(([id,icon,label])=>`<button class="nav-btn ${currentPage===id?'active':''}" data-page="${id}"><span class="nav-icon">${icon}</span>${label}</button>`).join('');$('sideNav').innerHTML=html;$('bottomNav').innerHTML=html;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{currentPage=b.dataset.page;document.querySelector('.sidebar').classList.remove('open');renderNav();renderPage()})}
function title(e,h){$('pageEyebrow').textContent=e;$('pageTitle').textContent=h} function progress(v){return `<div class="progress"><span style="width:${Math.max(0,Math.min(100,v))}%"></span></div>`} function renderPage(){session.role==='student'?renderStudent():renderTeacher()} function renderStudent(){({home:studentHome,workbook,progress:studentProgress,leaderboard:()=>leaderboard(false),profile}[currentPage]||studentHome)()} function renderTeacher(){({'teacher-home':teacherHome,students,classes,weaknesses,reports,'teacher-leaderboard':()=>leaderboard(true)}[currentPage]||teacherHome)()}
function attempts(sid,skill=null,lid=null){return getDB().attempts.filter(a=>a.studentId===sid&&(!skill||a.skill===skill)&&(!lid||a.lessonId===lid))} function mastery(sid,skill){const db=getDB(),base=db.profiles?.[sid]?.base?.[skill]??60,a=attempts(sid,skill);if(!a.length)return base;const best={};a.forEach(x=>best[x.lessonId]=Math.max(best[x.lessonId]||0,x.score));const vals=Object.values(best),avg=Math.round(vals.reduce((p,c)=>p+c,0)/vals.length);return Math.round(base*.35+avg*.65)} function overall(sid){return Math.round(['vocabulary','grammar','listening','writing'].reduce((a,k)=>a+mastery(sid,k),0)/4)} function points(sid){return getDB().profiles?.[sid]?.points||0}
function completionFor(sid,lid){return getDB().completion?.[sid]?.[lid]||[]} function lessonProgress(sid,lid){return Math.round(completionFor(sid,lid).length/5*100)} function completionPct(sid){return Math.round(COURSE.lessons.reduce((n,l)=>n+completionFor(sid,l.id).length,0)/(COURSE.lessons.length*5)*100)} function writingFor(sid,lid){return getDB().writing?.[sid]?.[lid]||''}
function weakest(sid){const r=['vocabulary','grammar','listening','writing'].map(k=>[k,mastery(sid,k)]).sort((a,b)=>a[1]-b[1]),k=r[0][0],tips={vocabulary:'Retrieve useful expressions without looking at the language bank.',grammar:'Build accurate sentences and connectors before you speak.',listening:'Listen for the main idea first, then details and sequence.',writing:'Organise a clear point, reason and example.'};return{key:k,label:cap(k),score:r[0][1],tip:tips[k]}}
function skillCard(n,v,d){return `<div class="card metric-card"><div class="metric-top"><span class="pill">${d}</span><strong>${v}%</strong></div><h4 style="margin:0">${n}</h4>${progress(v)}</div>`} function metric(l,v,s){return `<div class="card metric-card"><span class="pill">${s}</span><strong>${v}</strong><h4 style="margin:0">${l}</h4></div>`}
function studentHome(){const sid=session.id,w=weakest(sid),next=COURSE.lessons.find(l=>completionFor(sid,l.id).length<5)||COURSE.lessons[2];title('Student dashboard','Home');$('content').innerHTML=`<section class="hero"><div><p class="eyebrow">Week 1 · Fluency Foundations</p><h1>Good to see you, ${escapeHtml(session.name.split(' ')[0])}</h1><p>Your live class is for speaking. Use this workbook to strengthen vocabulary, grammar, listening and writing before class.</p></div><div class="hero-action"><small>Continue learning</small><strong>Lesson ${next.number} · ${next.title}</strong><button class="secondary-btn" id="continueBtn">Continue workbook →</button></div></section><section class="section"><div class="section-head"><div><h3>Your skills</h3><p>Current mastery estimate from your practice.</p></div><span class="pill teal">Overall ${overall(sid)}%</span></div><div class="grid grid-4">${skillCard('Vocabulary',mastery(sid,'vocabulary'),'Words & expressions')}${skillCard('Grammar',mastery(sid,'grammar'),'Sentence accuracy')}${skillCard('Listening',mastery(sid,'listening'),'Meaning & detail')}${skillCard('Writing',mastery(sid,'writing'),'Clear written ideas')}</div></section><section class="section grid grid-2"><div class="card"><h3>Your focus this week</h3><div class="weakness"><strong>${w.label} · ${w.score}%</strong><span>${w.tip}</span></div><button class="secondary-btn" id="practiceWeakBtn">Practise this skill</button></div><div class="card"><h3>Your course progress</h3><strong style="font-size:2.2rem">${completionPct(sid)}%</strong>${progress(completionPct(sid))}<div class="muted" style="margin-top:10px">${points(sid)} points earned</div></div></section>`;$('continueBtn').onclick=()=>{activeLessonId=next.id;currentStep=firstOpenStep(sid,next.id);currentPage='workbook';renderNav();workbook()};$('practiceWeakBtn').onclick=()=>{currentStep=w.key;currentPage='workbook';renderNav();workbook()}}
function firstOpenStep(sid,lid){return ['vocabulary','listening','grammar','writing','review'].find(s=>!completionFor(sid,lid).includes(s))||'review'}
function workbook(){const sid=session.id,l=lesson(),steps=[['vocabulary','1','Vocabulary'],['listening','2','Listening'],['grammar','3','Grammar'],['writing','4','Writing'],['review','5','Review']];title('Digital workbook',`Week 1 · Lesson ${l.number}`);$('content').innerHTML=`<section class="lesson-picker"><div><p class="eyebrow">Week 1 · 3 lessons</p><h3>Fluency Foundations</h3></div><div class="lesson-tabs">${COURSE.lessons.map(x=>`<button class="lesson-tab ${x.id===activeLessonId?'active':''}" data-lesson="${x.id}"><span>Lesson ${x.number}</span><strong>${x.title}</strong><small>${lessonProgress(sid,x.id)}% complete</small></button>`).join('')}</div></section><section class="section"><div class="section-head"><div><span class="pill teal">${COURSE.level}</span><h3 style="margin-top:8px">${l.title}</h3><p>${l.outcome}</p></div><strong>${lessonProgress(sid,l.id)}% complete</strong></div><div class="activity-layout"><div class="activity-steps">${steps.map(([id,n,label])=>`<button class="step-btn ${currentStep===id?'active':''}" data-step="${id}"><span>${n}. ${label}</span><span>${completionFor(sid,l.id).includes(id)?'✓':''}</span></button>`).join('')}</div><div id="activityPanel" class="activity-card"></div></div></section>`;document.querySelectorAll('[data-lesson]').forEach(b=>b.onclick=()=>{activeLessonId=b.dataset.lesson;currentStep=firstOpenStep(sid,activeLessonId);workbook()});document.querySelectorAll('[data-step]').forEach(b=>b.onclick=()=>{currentStep=b.dataset.step;workbook()});renderActivity()}
function renderActivity(){const p=$('activityPanel'),l=lesson();if(currentStep==='vocabulary')p.innerHTML=vocabActivity(l);if(currentStep==='listening')p.innerHTML=listeningActivity(l);if(currentStep==='grammar')p.innerHTML=grammarActivity(l);if(currentStep==='writing')p.innerHTML=writingActivity(l);if(currentStep==='review')p.innerHTML=reviewActivity(l);wireActivity(l)}
function vocabActivity(l){const qs=l.expressions.slice(0,4),opts=l.expressions.map(x=>x.text);return `<p class="eyebrow">Step 1 · Vocabulary</p><h3>Useful expressions</h3><p class="muted">Start with recognition. Match each communication job to the expression that does it.</p>${qs.map((x,i)=>`<div class="question"><p>${i+1}. You ${x.job}.</p><select data-vocab="${i}" data-answer="${escapeAttr(x.text)}"><option value="">Choose an expression</option>${opts.map(o=>`<option>${o}</option>`).join('')}</select></div>`).join('')}<div id="activityFeedback"></div><button class="primary-btn" id="checkActivity">Check answers</button>`}
function listeningActivity(l){return `<p class="eyebrow">Step 2 · Listening</p><h3>Listen for meaning, then detail</h3><p class="muted">Play once for the overall message. Play again for detail.</p><div class="audio-box"><button id="playAudio" class="play-btn">▶</button><div><strong>${l.listening.title}</strong><div class="muted">Listen, then answer without reading a transcript.</div></div></div>${l.listening.questions.map((q,i)=>`<div class="question"><p>${i+1}. ${q.q}</p>${radio('l'+i,q.options,q.answer,q.tag)}</div>`).join('')}<div id="activityFeedback"></div><button class="primary-btn" id="checkActivity">Check listening</button>`}
function grammarActivity(l){return `<p class="eyebrow">Step 3 · Grammar</p><h3>Grammar for the mission</h3><p class="muted">${l.grammar.focus}</p>${l.grammar.items.map((q,i)=>`<div class="question"><p>${i+1}. ${q.q}</p>${radio('g'+i,q.options,q.answer,q.tag)}</div>`).join('')}<div id="activityFeedback"></div><button class="primary-btn" id="checkActivity">Check grammar</button>`}
function writingActivity(l){const text=writingFor(session.id,l.id);return `<p class="eyebrow">Step 4 · Writing</p><h3>Write before you speak</h3><p class="muted">${l.writing.prompt}</p><div class="teacher-note"><strong>Helpful language:</strong> ${l.writing.starters}</div><div class="writing-box" style="margin-top:16px"><textarea id="writingText" placeholder="Write here…">${escapeHtml(text)}</textarea><div id="wordCount" class="word-count">0 words · target ${l.writing.min}–${l.writing.max}</div></div><div id="activityFeedback"></div><button class="primary-btn" id="saveWriting">Save writing</button>`}
function reviewActivity(l){const w=weakest(session.id);return `<p class="eyebrow">Step 5 · Review</p><h3>Ready for your live class</h3><p class="muted">Your results decide what deserves another short review.</p><div class="grid grid-2"><div class="card"><span class="pill teal">Current focus</span><h3>${w.label} · ${w.score}%</h3><p>${w.tip}</p>${progress(w.score)}</div><div class="card"><span class="pill gold">Speaking preparation</span><h3>${l.review.mission}</h3><p>Planning keywords: <strong>${l.review.keywords}</strong></p></div></div><div class="recommendation section"><p class="eyebrow">Before the live lesson</p><h3>Complete the message, then improve it.</h3><p>Your teacher can use your workbook evidence to choose the most useful correction.</p></div><button class="primary-btn section" id="finishReview">Mark lesson ready</button>`}
function radio(name,options,answer,tag=''){return `<div class="choice-list">${options.map(o=>`<label class="choice"><input type="radio" name="${name}" value="${escapeAttr(o)}" data-answer="${escapeAttr(answer)}" data-tag="${escapeAttr(tag)}"> <span>${o}</span></label>`).join('')}</div>`}
function wireActivity(l){if($('playAudio'))$('playAudio').onclick=()=>speak(l.listening.text);if($('checkActivity'))$('checkActivity').onclick=checkCurrent;if($('writingText')){const t=$('writingText'),wc=$('wordCount'),count=()=>{const n=t.value.trim()?t.value.trim().split(/\s+/).length:0;wc.textContent=`${n} words · target ${l.writing.min}–${l.writing.max}`};t.oninput=count;count()}if($('saveWriting'))$('saveWriting').onclick=()=>saveWriting(l);if($('finishReview'))$('finishReview').onclick=async()=>{await markDone(session.id,l.id,'review');await refreshState();workbook()}}
async function saveWriting(l){const t=$('writingText'),words=t.value.trim()?t.value.trim().split(/\s+/).length:0,f=$('activityFeedback');if(words<l.writing.min){f.innerHTML=`<div class="feedback bad">Write at least ${l.writing.min} words so there is enough evidence to review.</div>`;return}const score=Math.min(100,60+Math.round(Math.min(words,l.writing.max)/4));try{await api(`/api/writing/${l.id}`,{method:'PUT',body:JSON.stringify({content:t.value,score})});await refreshState();f.innerHTML='<div class="feedback good">Saved. This writing sample is now part of your progress evidence.</div>';setTimeout(workbook,650)}catch(e){f.innerHTML=`<div class="feedback bad">${escapeHtml(e.message)}</div>`}}
async function checkCurrent(){let total=0,correct=0,tags=[];if(currentStep==='vocabulary'){document.querySelectorAll('[data-vocab]').forEach(el=>{total++;if(el.value===el.dataset.answer)correct++;tags.push('vocabulary:expression-retrieval')})}else{const groups=[...new Set([...document.querySelectorAll('input[type=radio]')].map(x=>x.name))];total=groups.length;groups.forEach(g=>{const c=document.querySelector(`input[name="${g}"]:checked`);if(c){if(c.value===c.dataset.answer)correct++;if(c.dataset.tag)tags.push(c.dataset.tag)}})}const score=Math.round(correct/Math.max(1,total)*100);$('activityFeedback').innerHTML=`<div class="feedback ${score>=70?'good':'bad'}">${correct}/${total} correct · ${score}%. ${score>=70?'Good work. Your progress has been saved.':'Review the examples and try again.'}</div>`;try{await recordAttempt(session.id,activeLessonId,currentStep,score,tags);if(score>=70)await markDone(session.id,activeLessonId,currentStep);await refreshState();if(score>=70)setTimeout(workbook,800)}catch(e){$('activityFeedback').innerHTML=`<div class="feedback bad">Could not save progress: ${escapeHtml(e.message)}</div>`}}
async function recordAttempt(studentId,lessonId,skill,score,tags=[]){await api('/api/attempts',{method:'POST',body:JSON.stringify({lessonId,skill,score,tags:[...new Set(tags)]})})}
async function markDone(studentId,lid,step){await api('/api/completion',{method:'POST',body:JSON.stringify({lessonId:lid,step})})}
function speak(text){if(!('speechSynthesis'in window))return alert('Audio playback is not supported in this browser.');speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.88;u.lang='en-US';speechSynthesis.speak(u)}
function studentProgress(){const sid=session.id,w=weakest(sid),ats=attempts(sid).slice(-8).reverse();title('Learning analytics','My progress');$('content').innerHTML=`<section class="grid grid-4">${['vocabulary','grammar','listening','writing'].map(k=>skillCard(cap(k),mastery(sid,k),mastery(sid,k)>=75?'On track':'Keep practising')).join('')}</section><section class="section grid grid-2"><div class="card"><h3>Week 1 lesson progress</h3>${COURSE.lessons.map(l=>`<div class="question"><div style="display:flex;justify-content:space-between"><strong>Lesson ${l.number} · ${l.title}</strong><span>${lessonProgress(sid,l.id)}%</span></div>${progress(lessonProgress(sid,l.id))}</div>`).join('')}</div><div class="card"><h3>What to review next</h3><div class="weakness"><strong>${w.label} · ${w.score}%</strong><span>${w.tip}</span></div><p class="teacher-note">Your weakness data is private and never appears on the class leaderboard.</p></div></section><section class="section card"><h3>Recent evidence</h3>${ats.length?ats.map(a=>`<div class="attempt-row"><div><strong>${lesson(a.lessonId).title} · ${cap(a.skill)}</strong><span>${new Date(a.at).toLocaleString()}</span></div><strong>${a.score}%</strong></div>`).join(''):'<p class="muted">Complete an activity to start your evidence history.</p>'}</section>`}
function profile(){const u=getDB().users.find(x=>x.id===session.id)||session;title('Account','Profile');$('content').innerHTML=`<div class="grid grid-2"><div class="card"><h3>${escapeHtml(u.name)}</h3><p class="muted">Fluency Foundations · ${COURSE.level}</p><div class="question"><p>Username</p><span>${escapeHtml(u.username)}</span></div><div class="question"><p>Teacher</p><span>Teacher Ahmed</span></div><button class="ghost-btn" onclick="alert('Ask your teacher to reset your password.')">Request password reset</button></div><div class="card"><h3>Learning targets</h3><p>Week 1: build a baseline, introduce yourself professionally, and speak with fewer long pauses.</p><p>Next: maintain conversations, work updates and career communication.</p></div></div>`}
function classStudents(){return getDB().users.filter(u=>u.role==='student'&&u.classIds?.includes('c1'))}
function teacherHome(){const sts=classStudents(),avgComp=Math.round(sts.reduce((a,s)=>a+completionPct(s.id),0)/sts.length),priority=sts.filter(s=>weakest(s.id).score<65),cw=aggregateWeaknesses()[0];title('Teacher dashboard','Class overview');$('content').innerHTML=`<section class="hero"><div><p class="eyebrow">Fluency Foundations</p><h1>Teach what the evidence says matters.</h1><p>Workbook practice happens outside class. Your dashboard turns attempts into revision priorities for the next live speaking lesson.</p></div><div class="hero-action"><small>Next class recommendation</small><strong>${cw?.label||'Collect more evidence'}</strong><button class="secondary-btn" id="openWeak">View weakness map →</button></div></section><section class="section grid grid-4">${metric('Students',sts.length,'Enrolled')}${metric('Active',sts.filter(s=>attempts(s.id).length>0).length,'Has practice evidence')}${metric('Completion',avgComp+'%','Week 1 average')}${metric('Need support',priority.length,'Below 65% in one skill')}</section><section class="section grid grid-2"><div class="card"><h3>Needs your attention</h3>${priority.length?priority.map(s=>`<div class="weakness"><strong>${s.name} · ${weakest(s.id).label} ${weakest(s.id).score}%</strong><span>${weakest(s.id).tip}</span></div>`).join(''):'<p class="muted">No students are currently below the support threshold.</p>'}</div><div class="recommendation"><p class="eyebrow">Recommended revision</p><h3>${cw?.label||'Collect more practice data'}</h3><p>${cw?`${cw.count} student${cw.count!==1?'s':''} currently need this area most. Open the weakness map before your next live class.`:'Ask students to complete Week 1 practice so the dashboard can identify a class pattern.'}</p><button class="secondary-btn" id="openStudents">See students</button></div></section>`;$('openWeak').onclick=()=>{currentPage='weaknesses';renderNav();weaknesses()};$('openStudents').onclick=()=>{currentPage='students';renderNav();students()}}
function students(){const sts=classStudents();title('Class management','Students');$('content').innerHTML=`<div class="section-head"><div><h3>Fluency Foundations</h3><p>Create student access and review progress evidence.</p></div><button class="primary-btn" id="addStudent">+ Add student</button></div><div class="card table-wrap"><table class="data-table"><thead><tr><th>Student</th><th>Completion</th><th>Vocabulary</th><th>Grammar</th><th>Listening</th><th>Writing</th><th>Focus</th><th></th></tr></thead><tbody>${sts.map(s=>studentRow(s)).join('')}</tbody></table></div><p class="teacher-note section">Accounts, attempts, writing samples and mastery evidence are stored in Railway PostgreSQL. Teachers can reset passwords but cannot view current passwords.</p>`;$('addStudent').onclick=openAddStudent;document.querySelectorAll('[data-review-student]').forEach(b=>b.onclick=()=>openStudentDetail(b.dataset.reviewStudent))}
function studentRow(s){const w=weakest(s.id);return `<tr><td><div class="student-cell"><div class="avatar">${s.name[0]}</div><div><strong>${escapeHtml(s.name)}</strong><div class="muted">@${escapeHtml(s.username)}</div></div></div></td><td>${completionPct(s.id)}%</td><td>${mastery(s.id,'vocabulary')}%</td><td>${mastery(s.id,'grammar')}%</td><td>${mastery(s.id,'listening')}%</td><td>${mastery(s.id,'writing')}%</td><td>${w.label} ${w.score}%</td><td><button class="ghost-btn" data-review-student="${s.id}">Review</button></td></tr>`}
function openAddStudent(){showModal(`<div class="section-head"><div><p class="eyebrow">Teacher action</p><h3>Add student</h3></div><button class="icon-btn" data-close>×</button></div><form id="studentForm" class="form-grid"><label>Student name<input id="newName" required></label><label>Username<input id="newUsername" required placeholder="e.g. ahmed.ali"></label><label>Class<select id="newClass"><option value="c1">Fluency Foundations</option></select></label><button class="primary-btn" type="submit">Create student access</button></form><div id="createResult"></div>`);document.querySelector('[data-close]').onclick=closeModal;$('studentForm').onsubmit=async e=>{e.preventDefault();const name=$('newName').value.trim(),username=$('newUsername').value.trim().toLowerCase();try{const r=await api('/api/teacher/students',{method:'POST',body:JSON.stringify({name,username,classId:'c1'})});await refreshState();$('createResult').innerHTML=`<div class="feedback good"><strong>Student created.</strong><br>Username: <code>${escapeHtml(r.username)}</code><br>Temporary password: <code>${escapeHtml(r.temporaryPassword)}</code><br><small>Share it privately. The current password is never shown again.</small></div>`}catch(err){$('createResult').innerHTML=`<div class="feedback bad">${escapeHtml(err.message)}</div>`}}}
function openStudentDetail(id){const db=getDB(),s=db.users.find(u=>u.id===id),w=weakest(id),ats=attempts(id).slice(-10).reverse();showModal(`<div class="section-head"><div><p class="eyebrow">Student review</p><h3>${escapeHtml(s.name)}</h3></div><button class="icon-btn" data-close>×</button></div><div class="grid grid-4 compact-grid">${['vocabulary','grammar','listening','writing'].map(k=>skillCard(cap(k),mastery(id,k),'Mastery')).join('')}</div><div class="weakness section"><strong>Priority: ${w.label} · ${w.score}%</strong><span>${w.tip}</span></div><div class="section-head section"><div><h3>Recent attempts</h3><p>Evidence from workbook activity.</p></div><button class="ghost-btn" id="resetPass">Reset password</button></div>${ats.length?ats.map(a=>`<div class="attempt-row"><div><strong>Lesson ${lesson(a.lessonId).number} · ${cap(a.skill)}</strong><span>${new Date(a.at).toLocaleString()}</span></div><strong>${a.score}%</strong></div>`).join(''):'<p class="muted">No activity attempts yet.</p>'}<div id="resetResult"></div>`);document.querySelector('[data-close]').onclick=closeModal;$('resetPass').onclick=async()=>{try{const r=await api(`/api/teacher/students/${id}/reset-password`,{method:'POST'});$('resetResult').innerHTML=`<div class="feedback good">Temporary password reset to <code>${escapeHtml(r.temporaryPassword)}</code>. Share it privately.</div>`}catch(e){$('resetResult').innerHTML=`<div class="feedback bad">${escapeHtml(e.message)}</div>`}}}
function classes(){const sts=classStudents();title('Course management','Classes');$('content').innerHTML=`<div class="card"><div class="section-head"><div><span class="pill teal">Active cohort</span><h3>Fluency Foundations</h3><p>${COURSE.level} · Week 1 · ${sts.length} students</p></div><strong>${Math.round(sts.reduce((a,s)=>a+completionPct(s.id),0)/sts.length)}% average completion</strong></div><div class="lesson-admin-grid">${COURSE.lessons.map(l=>{const avg=Math.round(sts.reduce((a,s)=>a+lessonProgress(s.id,l.id),0)/sts.length);return `<div class="card"><span class="pill">Lesson ${l.number}</span><h3>${l.title}</h3><p>${l.outcome}</p><strong>${avg}% class completion</strong>${progress(avg)}</div>`}).join('')}</div></div>`}
function aggregateWeaknesses(){const map={};classStudents().forEach(s=>{const w=weakest(s.id);map[w.key]=map[w.key]||{key:w.key,label:w.label,count:0,total:0};map[w.key].count++;map[w.key].total+=w.score});return Object.values(map).map(x=>({...x,avg:Math.round(x.total/x.count)})).sort((a,b)=>b.count-a.count||a.avg-b.avg)}
function weaknesses(){const areas=aggregateWeaknesses();title('Teacher intelligence','Weakness map');$('content').innerHTML=`<div class="grid grid-2"><div class="card"><h3>Class-wide weaknesses</h3><p class="muted">Prioritised by how many students have each skill as their lowest area.</p>${areas.map(a=>`<div class="question"><div style="display:flex;justify-content:space-between"><strong>${a.label}</strong><span class="pill ${a.avg<65?'gold':'teal'}">${a.count} student${a.count!==1?'s':''}</span></div><div style="margin-top:10px">${progress(a.avg)}</div><small class="muted">Average mastery ${a.avg}%</small></div>`).join('')}</div><div><div class="recommendation"><p class="eyebrow">Best use of live time</p><h3>${areas[0]?.label||'Collect more evidence'}</h3><p>${areas[0]?`Start with a 10-minute revision targeting ${areas[0].label.toLowerCase()}. Then return immediately to the live speaking mission.`:'Have students complete workbook activities first.'}</p></div><div class="card section"><h3>Student-level priorities</h3>${classStudents().map(s=>`<p><strong>${s.name}:</strong> ${weakest(s.id).label} ${weakest(s.id).score}%</p>`).join('')}</div></div></div>`}
function reports(){const sts=classStudents(),ats=getDB().attempts;title('Evidence','Reports');$('content').innerHTML=`<div class="grid grid-3">${metric('Workbook completion',Math.round(sts.reduce((a,s)=>a+completionPct(s.id),0)/sts.length)+'%','Week 1')}${metric('Average mastery',Math.round(sts.reduce((a,s)=>a+overall(s.id),0)/sts.length)+'%','4 tracked skills')}${metric('Attempts saved',ats.length,'Practice evidence')}</div><section class="section card"><div class="section-head"><div><h3>Evidence by student</h3><p>Completion, mastery and practice attempts.</p></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Student</th><th>Completion</th><th>Overall</th><th>Attempts</th><th>Writing samples</th><th>Primary focus</th></tr></thead><tbody>${sts.map(s=>`<tr><td>${s.name}</td><td>${completionPct(s.id)}%</td><td>${overall(s.id)}%</td><td>${attempts(s.id).length}</td><td>${Object.keys(getDB().writing?.[s.id]||{}).length}</td><td>${weakest(s.id).label}</td></tr>`).join('')}</tbody></table></div></section>`}
function leaderboard(teacher){title('Motivation','Class leaderboard');const data=classStudents().map(s=>({...s,points:points(s.id),completion:completionPct(s.id)})).sort((a,b)=>b.points-a.points);$('content').innerHTML=`<div class="card"><div class="section-head"><div><h3>Fluency Foundations · Week 1</h3><p>Points reward completion, attempts and successful practice — not English level alone.</p></div><span class="pill gold">Weekly</span></div>${data.map((s,i)=>`<div class="leader-row ${s.id===session.id&&!teacher?'me':''}"><div class="rank">${i+1}</div><div class="student-cell"><div class="avatar">${s.name[0]}</div><div><strong>${s.name}${s.id===session.id&&!teacher?' · You':''}</strong><div class="muted">${s.completion}% workbook complete</div></div></div><strong>${s.points} pts</strong></div>`).join('')}</div>`}
function showModal(html){let m=$('modalRoot');if(!m){m=document.createElement('div');m.id='modalRoot';document.body.appendChild(m)}m.innerHTML=`<div class="modal-backdrop"><div class="modal-card">${html}</div></div>`;m.querySelector('.modal-backdrop').onclick=e=>{if(e.target.classList.contains('modal-backdrop'))closeModal()}}
function closeModal(){const m=$('modalRoot');if(m)m.innerHTML=''}
function escapeAttr(s){return String(s).replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;')} function escapeHtml(s){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}
